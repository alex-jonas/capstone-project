import styled from 'styled-components/macro'

export default styled.div`
  display: grid;
  align-content: start;
  gap: 20px;
  background: var(--result-background);
  padding: 10px;
  padding-top: 20px;
`

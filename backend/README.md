# Getting Started with wandergold (backend)

## Installation

Run `composer install` in the project backend directory (`/backend`). To work with your own database and Google API Key create a `.env` file according to `.env.example`.
